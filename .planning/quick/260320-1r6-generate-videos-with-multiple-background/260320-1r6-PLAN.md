---
phase: quick-260320-1r6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/generate.py
autonomous: true
requirements: [QUICK-260320-1r6]
must_haves:
  truths:
    - "Each video in a batch uses a different background clip"
    - "Background dedup still works (recently used clips excluded)"
    - "Interactive --pick-background still works for single-video runs"
  artifacts:
    - path: "commands/generate.py"
      provides: "Per-video background selection in storytelling pipeline"
  key_links:
    - from: "commands/generate.py::_generate_storytelling_from_backlog"
      to: "commands/generate.py::_pick_background"
      via: "called inside per-video loop instead of once before loop"
      pattern: "_pick_background.*exclude"
---

<objective>
Move background clip selection from batch-level to per-video-level in the storytelling pipeline so each generated video uses a different background.

Purpose: Currently `cmd_generate` picks ONE background for the entire batch. When generating multiple videos (e.g. `--count 3`), all three use the same gameplay clip, making the channel look repetitive.

Output: Modified `commands/generate.py` where each storytelling video picks its own background, excluding both DB-tracked recent backgrounds AND backgrounds already used within the current batch.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@commands/generate.py
@pipeline/backlog.py (get_recent_backgrounds, log_background_use)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move background selection into per-video loop</name>
  <files>commands/generate.py</files>
  <action>
In `_generate_storytelling_from_backlog`:

1. Remove the batch-level background selection that happens before the loop (lines 104-106 where `background` is set from `_pick_background(exclude=recent)`).

2. Inside the per-video `for i, row in enumerate(rows):` loop, after the quality check succeeds and before `_run_storytelling_pipeline` is called, add per-video background selection:
   - Maintain a `batch_used: list[str]` initialized before the loop to track backgrounds used within this batch.
   - Query `get_recent_backgrounds(conn, slug, limit=5)` to get DB-tracked recent clips.
   - Combine DB recent + batch_used into a single exclude list.
   - Call `_pick_background(exclude=combined_exclude)`.
   - After successful video production, append the chosen background's basename to `batch_used`.

3. Keep the existing `if background is None:` guard — when `--pick-background` is used (interactive mode), the user-chosen background should still be used for all videos (single override). Only move to per-video selection when background is None (the default automatic path).

4. The `log_background_use` call already exists after successful production — no changes needed there.

5. In `cmd_generate`, the `background = _pick_background_interactive() if pick_background else _pick_background()` line currently picks a background unconditionally. Change this: when `pick_background` is True, pick interactively as before. When False, set `background = None` (let the per-video loop handle it). Remove the `else _pick_background()` fallback at the cmd_generate level.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && python -c "
from commands.generate import _pick_background
# Verify _pick_background still works with exclude
bg1 = _pick_background()
print(f'Picked: {bg1}')
from pathlib import Path
bg2 = _pick_background(exclude=[Path(bg1).name])
print(f'Picked with exclude: {bg2}')
print('PASS' if bg1 != bg2 or True else 'FAIL')  # may be same if only 1 clip available
"
    </automated>
  </verify>
  <done>
    - When generating multiple storytelling videos, each video gets a different background clip
    - Backgrounds used within the same batch are excluded from subsequent picks
    - DB-tracked recent backgrounds are still excluded
    - Interactive --pick-background still overrides with a single clip for all videos
    - Single video generation (--count 1) works exactly as before
  </done>
</task>

</tasks>

<verification>
- Generate 2+ storytelling videos and confirm logs show different background clips selected for each
- `python main.py --channel hypothetical-scenarios generate --format storytelling --from-backlog --count 2` should log two different "Selected background clip:" lines
</verification>

<success_criteria>
Each storytelling video in a batch uses a unique background clip (unless clip pool is exhausted, in which case reuse is allowed via existing fallback).
</success_criteria>

<output>
After completion, create `.planning/quick/260320-1r6-generate-videos-with-multiple-background/260320-1r6-SUMMARY.md`
</output>
