---
phase: quick-260319-m4r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis/profiler.py        # DELETE
  - analysis/fetcher.py         # DELETE
  - analysis/transcripts.py     # DELETE
  - analysis/ranker.py          # DELETE
  - analysis/visual.py          # DELETE
  - analysis/__init__.py        # DELETE
  - analysis/db.py              # MOVE get_connection/init_db to pipeline/db.py
  - commands/analyze.py         # DELETE
  - commands/generate.py        # Remove _load_style_profile, --profile paths, _generate_storytelling, _generate_tweets
  - commands/run_cycle.py       # Remove _load_style_profile import and profile usage
  - config.py                   # Remove STYLE_PROFILES_DIR, style_profile field
  - main.py                     # Remove analyze subcommand, --profile flag, related dispatch
  - formats/storytelling/generator.py  # Remove profile parameter from generate_story, adapt_reddit_post
  - formats/storytelling/quality.py    # Remove profile-based scoring (auto-pass or remove)
  - formats/tweets/generator.py        # Remove profile-based generate_tweet/generate_batch/generate_thread
  - formats/tweets/quality.py          # Remove profile-based scoring
  - pipeline/db.py              # NEW - relocated from analysis/db.py
  - pipeline/reddit_scraper.py  # Update import from analysis.db -> pipeline.db
  - pipeline/backlog.py         # Update docstring reference to analysis.db
  - formats/tweets/scraper.py   # Update import from analysis.db -> pipeline.db
  - commands/scrape.py          # Update import from analysis.db -> pipeline.db
  - commands/review.py          # Update import from analysis.db -> pipeline.db
  - tests/test_run_cycle.py     # Update analysis.db mock targets -> pipeline.db
  - tests/test_tweet_scraper_store.py  # Update analysis.db reference
  - tests/test_config_channels.py      # Remove style_profile tests
  - tests/test_cli_channel_flag.py     # Remove analyze command tests
  - tests/test_story_generator.py      # Update if profile refs exist
  - CLAUDE.md                   # Remove style profile references, analysis system docs, analyze command
autonomous: true
requirements: []
must_haves:
  truths:
    - "No Python file imports from analysis/ (directory deleted)"
    - "No CLI command 'analyze' exists"
    - "No --profile flag on generate command"
    - "No style_profile field on ChannelConfig"
    - "No style_profiles/ directory auto-created"
    - "get_connection() and init_db() still work from pipeline.db"
    - "All existing tests pass (minus deleted style profile tests)"
  artifacts:
    - path: "pipeline/db.py"
      provides: "get_connection and init_db relocated from analysis/db.py"
      exports: ["get_connection", "init_db"]
    - path: "commands/generate.py"
      provides: "Video generation without any profile concept"
    - path: "main.py"
      provides: "CLI without analyze command or --profile flag"
  key_links:
    - from: "commands/run_cycle.py"
      to: "pipeline/db.py"
      via: "get_connection import"
      pattern: "from pipeline\\.db import get_connection"
    - from: "commands/generate.py"
      to: "formats/storytelling/generator.py"
      via: "adapt_reddit_post without profile arg"
    - from: "formats/tweets/scraper.py"
      to: "pipeline/db.py"
      via: "get_connection import"
      pattern: "from pipeline\\.db import get_connection"
---

<objective>
Remove the entire style profiling feature: the analysis/ directory (except db.py which gets relocated), the analyze CLI command, the --profile flag, style_profile config field, and all profile-related code paths in generators/quality checkers. Update all imports from analysis.db to pipeline.db.

Purpose: Eliminate dead/unused feature code. The pipeline uses backlog-based content (scraped Reddit posts and tweets), not AI-generated content from style profiles.
Output: Clean codebase with no style profile references, all tests passing.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@config.py
@main.py
@commands/generate.py
@commands/run_cycle.py
@commands/analyze.py
@analysis/db.py
@formats/storytelling/generator.py
@formats/storytelling/quality.py
@formats/tweets/generator.py
@formats/tweets/quality.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate analysis/db.py to pipeline/db.py, delete analysis/ directory and commands/analyze.py, update all imports</name>
  <files>
    pipeline/db.py (NEW),
    analysis/ (DELETE entire directory),
    commands/analyze.py (DELETE),
    pipeline/reddit_scraper.py,
    pipeline/backlog.py,
    formats/tweets/scraper.py,
    commands/scrape.py,
    commands/review.py,
    commands/run_cycle.py,
    commands/generate.py,
    tests/test_run_cycle.py,
    tests/test_tweet_scraper_store.py,
    tests/test_cli_channel_flag.py
  </files>
  <action>
1. Create `pipeline/db.py` with the exact contents of `analysis/db.py`. The file has `get_connection()`, `init_db()`, DB_PATH, and `from pipeline.backlog import init_backlog_tables` re-export. Keep all of this intact.

2. Delete the entire `analysis/` directory: `rm -rf analysis/`

3. Delete `commands/analyze.py`.

4. Global find-and-replace across all .py files: change every `from analysis.db import ...` to `from pipeline.db import ...`. Known files:
   - `pipeline/reddit_scraper.py` (line 183)
   - `formats/tweets/scraper.py` (line 586)
   - `commands/scrape.py` (lines 31, 53)
   - `commands/review.py` (line 75)
   - `commands/run_cycle.py` (line 42)
   - `commands/generate.py` (lines 132, 256)

5. Update `pipeline/backlog.py` docstring — change "Use analysis.db.get_connection()" to "Use pipeline.db.get_connection()".

6. In `tests/test_run_cycle.py`: replace all `patch("analysis.db.get_connection")` with `patch("pipeline.db.get_connection")`.

7. In `tests/test_tweet_scraper_store.py`: update the comment referencing analysis.db.

8. In `tests/test_cli_channel_flag.py`: remove test methods that test the `analyze` command (test_analyze_*, any method that calls `self._run(["analyze", ...])` or `self._run(["--channel", ..., "analyze", ...])`) since the command no longer exists.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && python -c "from pipeline.db import get_connection, init_db; print('pipeline.db OK')" && ! python -c "import analysis" 2>/dev/null && echo "analysis/ gone" && grep -r "from analysis" --include="*.py" . | grep -v __pycache__ | grep -v .planning; echo "EXIT:$?"</automated>
  </verify>
  <done>analysis/ directory deleted, pipeline/db.py works as drop-in replacement, all imports updated, no remaining "from analysis" imports in any .py file</done>
</task>

<task type="auto">
  <name>Task 2: Remove all style profile code from config, CLI, generators, quality checkers, and run_cycle</name>
  <files>
    config.py,
    main.py,
    commands/generate.py,
    commands/run_cycle.py,
    formats/storytelling/generator.py,
    formats/storytelling/quality.py,
    formats/tweets/generator.py,
    formats/tweets/quality.py,
    tests/test_config_channels.py,
    tests/test_story_generator.py
  </files>
  <action>
1. **config.py:**
   - Remove `STYLE_PROFILES_DIR` line and its entry in the `for _d in [...]` directory-creation loop.
   - Remove `style_profile: str = ""` from `ChannelConfig`.

2. **main.py:**
   - Remove the entire `# --- analyze ---` block (p_analyze and its arguments).
   - Remove `--profile` argument from p_gen.
   - Remove the `from commands.analyze import cmd_analyze` lazy import in `_dispatch_command`.
   - Remove the `if args.command == "analyze":` dispatch block.
   - Remove the validation block `if not args.scrape and not args.from_backlog and not args.profile:` — now generate always requires --scrape or --from-backlog (or just remove the check entirely since --profile is gone; the generate command should require either --scrape or --from-backlog).
   - Update the module docstring to remove "analyze" from the commands list.

3. **commands/generate.py:**
   - Remove `_load_style_profile()` function entirely.
   - Remove `profile_path` parameter from `cmd_generate`. Remove the profile-loading block at the top.
   - Remove `_generate_storytelling()` function (the one that takes profile/profile_path — this is the pure AI-generation path, not backlog). It is only used when profile_path is passed.
   - Remove `_generate_tweets()` function (the one that takes profile/profile_path/thread — pure AI-generation path).
   - In `cmd_generate`: remove the `profile_path` code paths. The function should now only support `from_backlog` and `scrape` modes. For storytelling: only `_generate_storytelling_from_backlog`. For tweets: only `scrape` or `_generate_tweets_from_backlog`.
   - In `_generate_storytelling_from_backlog`: remove the `profile = _load_style_profile(channel_cfg)` call. Pass `profile=None` to `adapt_reddit_post` calls (or remove the profile parameter). The quality lambda already handles None profile by auto-passing.
   - Remove the `_load_style_profile` import from the run_cycle imports at top if it was imported there.

4. **commands/run_cycle.py:**
   - Remove `_load_style_profile` from the import block at top.
   - Remove `profile = _load_style_profile(channel_cfg)` line (around line 90).
   - Update `adapt_reddit_post` call to not pass profile (or pass None).
   - Update quality lambda to always auto-pass: `lambda s: {"passed": True, "overall": 10.0}`.

5. **formats/storytelling/generator.py:**
   - Remove `generate_story(profile, ...)` function (AI-from-profile generation).
   - Remove `generate_batch(count, profile_path)` function.
   - In `adapt_reddit_post`: remove `profile` parameter. Remove the branch that uses profile to override niche defaults — always use niche defaults (the `_NICHE_TONES` dict). Update `_build_reddit_prompt` or equivalent to not accept profile.
   - Update module docstring to only show adapt_reddit_post.

6. **formats/storytelling/quality.py:**
   - Remove profile-based scoring. Since quality check auto-passes when no profile is provided (per existing decision), either: (a) make `check_quality` always return `{"passed": True, "overall": 10.0}`, or (b) delete the file entirely if it has no non-profile logic. Keep the file if it has word-count or other non-profile checks; delete if it only does profile-based Claude scoring.

7. **formats/tweets/generator.py:**
   - Remove `generate_tweet(profile, ...)` function.
   - Remove `generate_batch(count, profile_path)` function.
   - Remove `generate_thread(count, profile_path)` function.
   - If nothing remains (all functions were profile-based AI generation), delete the file. The tweet pipeline uses real scraped tweets, not AI-generated ones.

8. **formats/tweets/quality.py:**
   - Similar to storytelling quality — if it only does profile-based scoring, delete it. If it has non-profile checks (word count, etc), keep those.

9. **tests/test_config_channels.py:**
   - Remove `test_style_profile_optional` and `test_style_profile_set` test methods.

10. **tests/test_story_generator.py:**
    - Review and remove any tests that test generate_story or generate_batch (profile-based). Keep tests for adapt_reddit_post but remove profile parameter usage.

11. **Remove style_profiles/ directory:** `rm -rf style_profiles/`
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && grep -rn "style.profile\|STYLE_PROFILES\|_load_style_profile\|build_profile\|--profile" --include="*.py" . | grep -v __pycache__ | grep -v .planning; echo "---"; python -c "import config; print('config OK'); assert not hasattr(config, 'STYLE_PROFILES_DIR'), 'STYLE_PROFILES_DIR still exists'" && echo "config clean"</automated>
  </verify>
  <done>No style_profile references remain in any .py file. ChannelConfig has no style_profile field. No --profile CLI flag. No style_profiles/ directory. generators only support backlog/scrape paths.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md, run full test suite, verify clean state</name>
  <files>
    CLAUDE.md
  </files>
  <action>
1. **CLAUDE.md** updates:
   - In "Current State > Built and working": Remove "Channel analysis system (YouTube API -> transcripts -> style profiles)" line.
   - In "Building next": Remove "AI content generation (stories + tweets) guided by style profiles" and "Quality checking" lines (or update to reflect current state).
   - In "Project Structure": Remove the entire `analysis/` directory tree. Remove `style_profiles/` line. Add `pipeline/db.py` entry with comment "SQLite connection + schema init".
   - In "CLI Commands": Remove the entire "Analyze channels" section (`python main.py analyze ...`). Remove the "Generate videos from style profile" examples. Keep only scrape/backlog/run-cycle examples.
   - Remove the entire "Style Profile Schema" section.
   - In the CLI runner line at top: remove "analyze" from the commands list.

2. Run the full test suite to verify nothing is broken:
   ```
   cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && python -m pytest tests/ -v
   ```

3. Fix any test failures caused by the refactoring.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && python -m pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>CLAUDE.md has no style profile or analysis references. Full test suite passes. No orphaned imports or dead code.</done>
</task>

</tasks>

<verification>
1. `grep -rn "style.profile\|profiler\|analyze\|analysis\." --include="*.py" . | grep -v __pycache__ | grep -v .planning` returns nothing (or only unrelated uses of the word "analyze" in comments/logs)
2. `python -m pytest tests/ -v` all tests pass
3. `python main.py --help` shows no "analyze" command
4. `python main.py --channel X generate --help` shows no --profile flag
5. `ls analysis/ 2>/dev/null` fails (directory gone)
6. `ls style_profiles/ 2>/dev/null` fails (directory gone)
7. `python -c "from pipeline.db import get_connection; get_connection()"` works
</verification>

<success_criteria>
- Zero references to style profiles, profiler, or analysis/ in any .py file
- analysis/ directory deleted
- style_profiles/ directory deleted
- commands/analyze.py deleted
- pipeline/db.py provides get_connection() and init_db()
- All tests pass
- CLAUDE.md updated to reflect current state
- CLI has no analyze command or --profile flag
</success_criteria>

<output>
After completion, create `.planning/quick/260319-m4r-remove-the-style-profiling-feature-entir/260319-m4r-SUMMARY.md`
</output>
