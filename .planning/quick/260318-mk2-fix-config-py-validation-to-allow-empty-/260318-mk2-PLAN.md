---
phase: quick-260318-mk2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config.py]
autonomous: true
requirements: [QUICK-FIX]

must_haves:
  truths:
    - "Tweets-format channels with empty subreddits load without error"
    - "Storytelling-format channels still require non-empty subreddits"
  artifacts:
    - path: "config.py"
      provides: "Format-conditional subreddits validation"
      contains: "format.*storytelling.*subreddits"
  key_links: []
---

<objective>
Fix config.py validation so tweets-format channels can have an empty subreddits list.

Purpose: The viral-tweets channel (format: tweets) only scrapes X.com and never uses Reddit. The current unconditional `subreddits must not be empty` check in `ChannelConfig.__post_init__` blocks it from loading.
Output: Updated config.py where subreddits validation is conditional on format=storytelling.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make subreddits validation conditional on storytelling format</name>
  <files>config.py</files>
  <action>
In `ChannelConfig.__post_init__` (line ~111), change the unconditional subreddits check:

```python
if not self.subreddits:
    raise ValueError(...)
```

To be conditional on the channel format:

```python
if self.format == "storytelling" and not self.subreddits:
    raise ValueError(
        f"Channel '{self.slug}': subreddits list must not be empty for storytelling format"
    )
```

This ensures storytelling channels still require subreddits (they need Reddit content), while tweets-format channels can have `subreddits: []` since they only scrape X.com.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && python -c "
from config import ChannelConfig
# Tweets format with empty subreddits should work
c = ChannelConfig(slug='test-tweets', name='Test', format='tweets', voice_id='abc123', subreddits=[], twitter_accounts=['@test'])
print(f'OK: tweets channel loaded: {c.slug}')

# Storytelling format with empty subreddits should fail
try:
    c2 = ChannelConfig(slug='test-story', name='Test', format='storytelling', voice_id='abc123', subreddits=[], twitter_accounts=[])
    print('FAIL: storytelling channel with empty subreddits should have raised ValueError')
except ValueError as e:
    print(f'OK: storytelling validation still works: {e}')
"</automated>
  </verify>
  <done>Tweets-format channels load with empty subreddits; storytelling channels still require non-empty subreddits</done>
</task>

</tasks>

<verification>
Run `python main.py --channel viral-tweets scrape --format tweets` — should no longer raise ValueError about subreddits.
</verification>

<success_criteria>
- config.py validates subreddits only for storytelling format
- Existing storytelling channels unaffected
- viral-tweets channel loads and runs scrape command without subreddits error
</success_criteria>

<output>
After completion, create `.planning/quick/260318-mk2-fix-config-py-validation-to-allow-empty-/260318-mk2-SUMMARY.md`
</output>
