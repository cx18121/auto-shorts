---
phase: quick-260318-n9g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - formats/storytelling/assembler.py
  - formats/tweets/assembler.py
  - main.py
autonomous: true
requirements: [CUTOFF-FIX, TWEET-INTRO]

must_haves:
  truths:
    - "Storytelling videos play the full narration without clipping the last word"
    - "Tweet videos play the full narration without clipping the last word"
    - "Tweet voiceover begins with @username says before the tweet text"
  artifacts:
    - path: "formats/storytelling/assembler.py"
      provides: "Duration buffer on all adjusted_duration computations"
      contains: "+ 0.5"
    - path: "formats/tweets/assembler.py"
      provides: "Duration buffer on adjusted_duration"
      contains: "+ 0.5"
    - path: "main.py"
      provides: "TTS text prefixed with @username says"
      contains: "says:"
  key_links:
    - from: "formats/storytelling/assembler.py"
      to: "FFmpeg -t flag"
      via: "adjusted_duration + 0.5 passed to -t"
      pattern: "adjusted_duration.*\\+ 0\\.5"
    - from: "formats/tweets/assembler.py"
      to: "FFmpeg -t flag"
      via: "adjusted_duration + 0.5 passed to -t"
      pattern: "adjusted_duration.*\\+ 0\\.5"
    - from: "main.py _scrape_tweets"
      to: "generate_tts call"
      via: "f-string prepending @username says:"
      pattern: "says:.*tweet"
    - from: "main.py _run_tweet_pipeline"
      to: "generate_tts call"
      via: "f-string prepending @username says:"
      pattern: "says:.*tweet"
---

<objective>
Fix video audio cutoff in both storytelling and tweet assemblers by adding +0.5s buffer to adjusted_duration, and add @username says: intro to tweet TTS voiceover.

Purpose: Videos currently clip the last word because adjusted_duration is fractionally too short after speed division. Tweet videos also lack speaker attribution.
Output: Patched assemblers (both formats) and main.py TTS call sites.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260318-n9g-fix-tweet-videos-cutting-off-at-end-and-/260318-n9g-CONTEXT.md
@formats/storytelling/assembler.py
@formats/tweets/assembler.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add +0.5s buffer to adjusted_duration in both assemblers</name>
  <files>formats/storytelling/assembler.py, formats/tweets/assembler.py</files>
  <action>
In formats/storytelling/assembler.py, add `+ 0.5` to every `adjusted_duration` computation. There are three locations:

1. `assemble_video()` (line 85): change
   `adjusted_duration = duration_seconds / AUDIO_SPEED`
   to `adjusted_duration = duration_seconds / AUDIO_SPEED + 0.5`

2. `_build_ffmpeg_cmd()` (line 122): change
   `adjusted_duration = duration / AUDIO_SPEED`
   to `adjusted_duration = duration / AUDIO_SPEED + 0.5`

3. `_build_split_ffmpeg_cmd()` (line 355): change
   `adjusted_duration = duration / AUDIO_SPEED`
   to `adjusted_duration = duration / AUDIO_SPEED + 0.5`

In formats/tweets/assembler.py, add `+ 0.5` to the single location:

1. `assemble_tweet_video()` (line 74): change
   `adjusted_duration = duration_seconds / AUDIO_SPEED`
   to `adjusted_duration = duration_seconds / AUDIO_SPEED + 0.5`

Do NOT change to `-shortest`. Keep `-t` with the buffered duration. Do not change anything else in the assembler logic.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && grep -n '+ 0.5' formats/storytelling/assembler.py formats/tweets/assembler.py | wc -l | xargs -I{} test {} -eq 4 && echo "PASS: 4 buffer locations found" || echo "FAIL: expected 4 buffer locations"</automated>
  </verify>
  <done>All four adjusted_duration computations (3 in storytelling, 1 in tweets) include +0.5s buffer, preventing FFmpeg -t from clipping the last word.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend @username says: to tweet TTS text</name>
  <files>main.py</files>
  <action>
Modify the two locations in main.py where tweet text is passed to `generate_tts()`:

1. In `_scrape_tweets()` (around line 642): change
   `tts = generate_tts(tweet["text"], str(workdir))`
   to:
   ```python
   tts_script = f"@{tweet['username']} says: {tweet['text']}"
   tts = generate_tts(tts_script, str(workdir))
   ```

2. In `_run_tweet_pipeline()` (around line 594): change
   `tts = generate_tts(tweet["tweet_text"], str(workdir))`
   to:
   ```python
   tts_script = f"@{tweet.get('username', '')} says: {tweet['tweet_text']}"
   tts = generate_tts(tts_script, str(workdir))
   ```
   Use `.get('username', '')` here because this function handles backlog tweets where username could theoretically be missing.

No change needed at the `cmd_run_cycle` call site (~line 1176) — it calls `_run_tweet_pipeline` which is already patched above. The tweet dict at line 1169-1175 includes `"username": row["username"]`.

Do NOT change `content_text`, `_save_video_metadata`, or upload metadata calls — those should remain raw tweet text without the attribution prefix.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && grep -c "says:" main.py | xargs -I{} test {} -ge 2 && echo "PASS: says: pattern found in main.py" || echo "FAIL: says: pattern not found"</automated>
  </verify>
  <done>Both tweet TTS call sites prepend "@username says: " to the text sent to ElevenLabs TTS.</done>
</task>

</tasks>

<verification>
1. `grep -n '+ 0.5' formats/storytelling/assembler.py` — should show 3 lines
2. `grep -n '+ 0.5' formats/tweets/assembler.py` — should show 1 line
3. `grep -n 'says:' main.py` — should show 2 lines in _scrape_tweets and _run_tweet_pipeline
4. No other logic changed in either assembler (diff confirms only + 0.5 additions)
</verification>

<success_criteria>
- Storytelling assembler has +0.5s buffer at all 3 adjusted_duration sites
- Tweet assembler has +0.5s buffer at its 1 adjusted_duration site
- Both tweet TTS call sites in main.py prepend "@username says: " to the narration script
- No other behavioral changes to assembler, pipeline, or metadata logic
</success_criteria>

<output>
After completion, create `.planning/quick/260318-n9g-fix-tweet-videos-cutting-off-at-end-and-/260318-n9g-SUMMARY.md`
</output>
