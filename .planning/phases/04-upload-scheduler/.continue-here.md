---
phase: 04-upload-scheduler
task: 0
total_tasks: 0
status: context_gathered
last_updated: 2026-03-12T16:57:08.762Z
---

<current_state>
Phase 4 context has been gathered and committed. CONTEXT.md is written with all user decisions. The phase is ready for planning — no plans have been created yet.
</current_state>

<completed_work>

- Phase 4 directory created: `.planning/phases/04-upload-scheduler/`
- Context discussion completed: all 4 gray areas discussed (scheduling, upload metadata, credential flow, run orchestration)
- `04-CONTEXT.md` written and committed (617c98e)
- STATE.md updated and committed (cf3ff68)
</completed_work>

<remaining_work>

- Run `/gsd:plan-phase 4` to create PLAN.md files (research → plan → verify)
- Execute the plans once created
- Phase 4 requirements to satisfy: UPLOAD-01, UPLOAD-02, UPLOAD-03, UPLOAD-04, UPLOAD-05, SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, MULTI-02
</remaining_work>

<decisions_made>

- System cron for scheduling (not APScheduler) — simplest, works locally and on VPS
- Separate cron jobs for posting (2x/day) and scraping (1x/day)
- `enabled: true/false` field in channels.yaml to pause channels
- Claude generates all video titles (both storytelling and tweets)
- Hashtags: base list from channels.yaml + 2-3 content-specific ones from Claude per video
- Same metadata for YouTube and Instagram (no platform-specific variants)
- CLI setup commands for both YouTube OAuth (`setup-youtube`) and Instagram (`setup-instagram`)
- OAuth tokens stored as JSON files in `data/channels/{slug}/` (gitignored)
- Skip + log + continue on auth errors at runtime
- One video per run-cycle, highest scored backlog item first
- Auto-trigger scrape if backlog is empty when run fires
</decisions_made>

<blockers>
- YouTube OAuth 2.0 and Instagram Graph API credentials must be configured before upload automation can be tested (noted in STATE.md)
</blockers>

<context>
Phase 4 is the final phase of v1. All prior phases (1-3) are complete. The pipeline can already scrape content, store it in a backlog, generate videos from backlog items, and produce final MP4s. This phase adds the last mile: uploading to platforms and automating the whole thing on a schedule.
</context>

<next_action>
Start with: `/gsd:plan-phase 4` (clear context first with /clear)
</next_action>
