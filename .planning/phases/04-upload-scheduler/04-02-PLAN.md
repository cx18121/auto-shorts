---
phase: 04-upload-scheduler
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - channels.yaml.example
  - main.py
  - tests/test_config_channels.py
autonomous: true
requirements:
  - SCHED-05
  - MULTI-02

must_haves:
  truths:
    - "ChannelConfig has enabled, hashtags, and instagram_user_id fields"
    - "channels.yaml.example documents enabled, hashtags, and instagram_user_id for all three niches"
    - "setup-youtube CLI opens OAuth flow and saves token to data/channels/{slug}/youtube_token.json"
    - "setup-instagram CLI walks through token setup and saves to data/channels/{slug}/instagram_token.json"
    - "Disabled channels (enabled: false) are skippable by downstream code"
  artifacts:
    - path: "config.py"
      provides: "ChannelConfig with enabled, hashtags, instagram_user_id fields"
      contains: "enabled"
    - path: "channels.yaml.example"
      provides: "Updated example config with new fields for all niches"
      contains: "enabled: true"
    - path: "main.py"
      provides: "setup-youtube and setup-instagram subcommands"
      contains: "setup-youtube"
  key_links:
    - from: "main.py:cmd_setup_youtube"
      to: "pipeline/upload.py:setup_youtube_oauth"
      via: "import and call"
      pattern: "setup_youtube_oauth"
    - from: "main.py:cmd_setup_instagram"
      to: "data/channels/{slug}/instagram_token.json"
      via: "writes token file"
      pattern: "instagram_token\\.json"

user_setup:
  - service: youtube
    why: "YouTube Data API v3 OAuth for upload"
    env_vars: []
    dashboard_config:
      - task: "Create OAuth 2.0 Client ID (Desktop app type) in Google Cloud Console"
        location: "Google Cloud Console -> APIs & Services -> Credentials"
      - task: "Enable YouTube Data API v3"
        location: "Google Cloud Console -> APIs & Services -> Library"
      - task: "Add youtube_client_id and youtube_client_secret to channels.yaml"
        location: "channels.yaml per-channel config"
      - task: "Complete YouTube API compliance audit (videos upload as private until audited)"
        location: "https://support.google.com/youtube/contact/yt_api_form"
  - service: instagram
    why: "Instagram Graph API for Reels publishing"
    env_vars: []
    dashboard_config:
      - task: "Ensure Instagram account is Business or Creator type"
        location: "Instagram App -> Settings -> Account -> Switch to Professional Account"
      - task: "Connect Instagram Business account to a Facebook Page"
        location: "Facebook Page Settings -> Instagram"
      - task: "Create a Meta App with instagram_content_publish permission"
        location: "developers.facebook.com -> My Apps"
      - task: "Generate a short-lived token and exchange for long-lived token"
        location: "Meta Graph API Explorer or setup-instagram CLI"
---

<objective>
Extend ChannelConfig with `enabled`, `hashtags`, and `instagram_user_id` fields needed by the upload and scheduling system. Add `setup-youtube` and `setup-instagram` CLI commands that configure OAuth credentials per channel. Update channels.yaml.example with all new fields.

Purpose: The upload module (Plan 01) and run-cycle orchestrator (Plan 03) need these config fields and auth setup commands to function. This plan ensures channels have all the configuration needed for automated uploads.
Output: Updated config.py, channels.yaml.example, main.py with setup commands, passing tests.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-upload-scheduler/04-CONTEXT.md
@.planning/phases/04-upload-scheduler/04-RESEARCH.md

@config.py
@channels.yaml.example
@main.py
@tests/test_config_channels.py

<interfaces>
<!-- Existing ChannelConfig from config.py -->
```python
@dataclass
class ChannelConfig:
    slug: str
    name: str
    format: str
    voice_id: str
    subreddits: list
    twitter_accounts: list
    youtube_client_id: str = ""
    youtube_client_secret: str = ""
    instagram_access_token: str = ""
    quality: dict = field(default_factory=dict)
    style_profile: str = ""
```

<!-- Existing main.py argparse structure -->
```python
# Subcommands: analyze, generate, setup-twitter, scrape, review, backlog-status
# _dispatch_command routes based on args.command
# --channel is a root parser argument
```

<!-- Token paths (from CONTEXT.md decision) -->
```
data/channels/{slug}/youtube_token.json
data/channels/{slug}/instagram_token.json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enabled, hashtags, instagram_user_id to ChannelConfig and update channels.yaml.example</name>
  <files>config.py, channels.yaml.example, tests/test_config_channels.py</files>
  <action>
    In config.py, add three new fields to the ChannelConfig dataclass (after instagram_access_token, before quality):
    - `enabled: bool = True` — channels are enabled by default
    - `hashtags: list = field(default_factory=list)` — base hashtags for uploads
    - `instagram_user_id: str = ""` — numeric IG Business account ID

    In load_channels(), update the ChannelConfig construction to pass these new fields from the YAML data. Use `raw.get("enabled", True)`, `raw.get("hashtags", [])`, `raw.get("instagram_user_id", "")`. No validation needed for these fields (empty is valid — means platform not configured).

    Update channels.yaml.example to add for each of the three niches:
    ```yaml
    enabled: true
    hashtags:
      - shorts
      - storytime  # or niche-appropriate tags
    instagram_user_id: ""  # numeric IG Business account ID from setup-instagram
    ```
    Place these after `instagram_access_token` and before `quality`.

    Update tests/test_config_channels.py: add a test that verifies enabled, hashtags, and instagram_user_id are loaded correctly from YAML. Add a test that missing enabled defaults to True. Add a test that missing hashtags defaults to empty list.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && python -m pytest tests/test_config_channels.py -x -q</automated>
  </verify>
  <done>ChannelConfig has enabled (bool, default True), hashtags (list, default []), instagram_user_id (str, default ""). channels.yaml.example documents all three fields. Existing tests still pass; new tests verify defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Add setup-youtube and setup-instagram CLI subcommands to main.py</name>
  <files>main.py</files>
  <action>
    Add two new subcommands to the argparse setup in main():

    1. `setup-youtube` subparser — no extra arguments needed (channel_cfg provides client_id/secret).
    2. `setup-instagram` subparser — optional `--token` argument for providing a short-lived token to exchange for long-lived.

    Add `cmd_setup_youtube(channel_cfg)`:
    - Import setup_youtube_oauth from pipeline.upload (lazy import inside function body).
    - Compute token_path = Path(config.CHANNELS_DIR) / channel_cfg.slug / "youtube_token.json".
    - If token_path exists, warn that it will be overwritten and ask for confirmation.
    - If youtube_client_id or youtube_client_secret is empty, print error and exit.
    - Call setup_youtube_oauth(channel_cfg, token_path).
    - Print success message and WARNING about YouTube API compliance audit: "NOTE: YouTube API projects created after July 2020 may have uploads locked to private until the GCP project passes a compliance audit. See: https://support.google.com/youtube/contact/yt_api_form"

    Add `cmd_setup_instagram(channel_cfg, token=None)`:
    - Compute token_path = Path(config.CHANNELS_DIR) / channel_cfg.slug / "instagram_token.json".
    - If token is provided: exchange short-lived token for long-lived token via GET https://graph.instagram.com/access_token?grant_type=ig_exchange_token&client_secret={app_secret}&access_token={short_lived}. Note: Use requests.get with timeout=10 and 3 retries.
    - If no token provided: print instructions for how to get a short-lived token from the Meta Graph API Explorer, then prompt user to paste it. Read from stdin. Exchange it.
    - After getting long-lived token: call GET https://graph.instagram.com/me?fields=id,username&access_token={long_lived} to fetch the numeric user ID.
    - Save {"access_token": token, "user_id": ig_user_id, "expires_at": (now + expires_in seconds).isoformat()} to token_path.
    - Print success message with the username and user ID.
    - Print reminder: "Ensure your Instagram account is a Business or Creator account connected to a Facebook Page."

    Wire both into _dispatch_command():
    ```python
    elif args.command == "setup-youtube":
        cmd_setup_youtube(channel_cfg)
    elif args.command == "setup-instagram":
        cmd_setup_instagram(channel_cfg, getattr(args, "token", None))
    ```
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && python main.py --help 2>&1 | grep -q "setup-youtube" && python main.py --help 2>&1 | grep -q "setup-instagram" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>setup-youtube and setup-instagram subcommands are wired into main.py argparse and _dispatch_command. Running --help shows both commands. cmd_setup_youtube calls pipeline.upload.setup_youtube_oauth with proper token path. cmd_setup_instagram exchanges tokens and saves to instagram_token.json.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_config_channels.py -x -q` — all tests pass including new field tests
- `python main.py --help` shows setup-youtube and setup-instagram subcommands
- `python -c "from config import ChannelConfig; c = ChannelConfig(slug='test', name='Test', format='storytelling', voice_id='v1', subreddits=['r/test'], twitter_accounts=[]); assert c.enabled == True; assert c.hashtags == []; assert c.instagram_user_id == ''"` — defaults work
</verification>

<success_criteria>
- ChannelConfig has enabled, hashtags, instagram_user_id fields with correct defaults
- channels.yaml.example documents all new fields for all three niches
- setup-youtube and setup-instagram CLI commands exist and are properly wired
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-upload-scheduler/04-02-SUMMARY.md`
</output>
