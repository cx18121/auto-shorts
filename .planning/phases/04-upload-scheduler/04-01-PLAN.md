---
phase: 04-upload-scheduler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/upload.py
  - tests/test_upload.py
  - requirements.txt
autonomous: true
requirements:
  - UPLOAD-01
  - UPLOAD-02
  - UPLOAD-03
  - UPLOAD-04
  - UPLOAD-05

must_haves:
  truths:
    - "upload_to_youtube() authenticates via saved OAuth token, uploads video, returns YouTube video ID"
    - "upload_to_instagram() creates container, polls status, publishes reel, returns IG media ID"
    - "generate_upload_metadata() calls Claude Haiku and returns title + hashtags as dict"
    - "Upload retries on 5xx errors with exponential backoff (3 retries)"
    - "log_upload() inserts a row into the uploads table with platform, video_id, title, status, timestamp"
    - "init_upload_table() creates the uploads DDL in SQLite"
  artifacts:
    - path: "pipeline/upload.py"
      provides: "YouTube upload, Instagram upload, metadata generation, DB logging, retry logic"
      exports: ["setup_youtube_oauth", "upload_to_youtube", "upload_to_instagram", "generate_upload_metadata", "init_upload_table", "log_upload", "get_upload_history", "refresh_instagram_token_if_needed"]
    - path: "tests/test_upload.py"
      provides: "Unit tests for all upload module functions"
      contains: "TestYouTubeUpload"
    - path: "requirements.txt"
      provides: "google-auth-oauthlib and google-auth-httplib2 dependencies"
      contains: "google-auth-oauthlib"
  key_links:
    - from: "pipeline/upload.py"
      to: "google-api-python-client"
      via: "build('youtube', 'v3', credentials=creds) + videos().insert()"
      pattern: "build.*youtube.*v3"
    - from: "pipeline/upload.py"
      to: "requests"
      via: "Instagram Graph API REST calls"
      pattern: "requests\\.(post|get).*graph\\.instagram"
    - from: "pipeline/upload.py"
      to: "anthropic"
      via: "Claude Haiku for metadata generation"
      pattern: "client\\.messages\\.create"
---

<objective>
Build the upload module (pipeline/upload.py) that handles YouTube Shorts upload via OAuth 2.0, Instagram Reels upload via Graph API, AI-generated upload metadata (title/hashtags), upload record logging to SQLite, and retry logic with exponential backoff.

Purpose: This is the core upload infrastructure that the run-cycle orchestrator (Plan 03) will call. All platform-specific upload logic lives here.
Output: pipeline/upload.py with all upload functions, tests/test_upload.py with full unit test coverage, updated requirements.txt.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-upload-scheduler/04-CONTEXT.md
@.planning/phases/04-upload-scheduler/04-RESEARCH.md

@config.py
@pipeline/backlog.py
@main.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From config.py:
```python
@dataclass
class ChannelConfig:
    slug: str
    name: str
    format: str
    voice_id: str
    subreddits: list
    twitter_accounts: list
    youtube_client_id: str = ""
    youtube_client_secret: str = ""
    instagram_access_token: str = ""
    quality: dict = field(default_factory=dict)
    style_profile: str = ""
```

From config.py:
```python
DATA_DIR: Path = Path(__file__).parent / "data"
CHANNELS_DIR: Path = DATA_DIR / "channels"
ANTHROPIC_API_KEY: str  # from .env
```

From analysis/db.py:
```python
def get_connection() -> sqlite3.Connection  # returns pipeline.db connection
```

From pipeline/backlog.py:
```python
def get_approved_stories(conn, channel: str) -> list  # rows with id, title, body, subreddit, score
def get_approved_tweets(conn, channel: str) -> list    # rows with id, tweet_text, author, likes, etc.
def mark_story_used(conn, story_id: str) -> None
def mark_used(conn, table: str, item_id: str) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pipeline/upload.py with YouTube upload, Instagram upload, metadata generation, DB logging, and retry logic</name>
  <files>pipeline/upload.py, tests/test_upload.py, requirements.txt</files>
  <behavior>
    - TestYouTubeSetup: setup_youtube_oauth() calls InstalledAppFlow.from_client_config with correct scopes and saves creds to token_path
    - TestYouTubeUpload: upload_to_youtube() loads creds from token file, refreshes if expired, calls videos().insert() with correct snippet (title, description, tags, categoryId=22) and status (privacyStatus=public), returns video ID from response
    - TestYouTubeUpload: upload_to_youtube() retries on 500/502/503/504 HttpError with exponential backoff, raises after MAX_RETRIES
    - TestInstagramUpload: upload_to_instagram() POSTs to /{ig_user_id}/media with media_type=REELS, polls container status, POSTs to /{ig_user_id}/media_publish, returns media ID
    - TestInstagramUpload: upload_to_instagram() raises RuntimeError on container ERROR status
    - TestInstagramUpload: upload_to_instagram() raises RuntimeError on container timeout (>5 min)
    - TestInstagramTokenRefresh: refresh_instagram_token_if_needed() refreshes token when expires_in < 7 days, writes updated token file
    - TestMetadataGeneration: generate_upload_metadata() calls Claude Haiku with content text and niche hashtags, returns {"title": str, "hashtags": list[str]}
    - TestUploadLogging: init_upload_table() creates uploads table; log_upload() inserts row with channel, platform, video_id, title, status, error_msg, uploaded_at
    - TestUploadHistory: get_upload_history() returns recent upload records filtered by channel
  </behavior>
  <action>
    Add `google-auth-oauthlib>=1.0` and `google-auth-httplib2>=0.1` to requirements.txt.

    Create pipeline/upload.py with these functions:

    1. `init_upload_table(conn)` — CREATE TABLE IF NOT EXISTS uploads (id INTEGER PRIMARY KEY AUTOINCREMENT, channel TEXT NOT NULL, platform TEXT NOT NULL, video_id TEXT NOT NULL, title TEXT NOT NULL, status TEXT NOT NULL, error_msg TEXT, uploaded_at TEXT NOT NULL). Follow the same pattern as init_backlog_tables in pipeline/backlog.py.

    2. `log_upload(conn, channel, platform, video_id, title, status, error_msg=None)` — INSERT into uploads table with datetime.now(timezone.utc).isoformat() for uploaded_at.

    3. `get_upload_history(conn, channel, limit=20)` — SELECT from uploads WHERE channel=? ORDER BY uploaded_at DESC LIMIT ?. Return list of dicts.

    4. `setup_youtube_oauth(channel_cfg, token_path: Path)` — Use InstalledAppFlow.from_client_config() with inline dict built from channel_cfg.youtube_client_id and youtube_client_secret. Scopes = ["https://www.googleapis.com/auth/youtube.upload"]. Call flow.run_local_server(port=0). Save creds.to_json() to token_path. Print warning about YouTube API compliance audit (videos may appear as private until GCP project is audited).

    5. `upload_to_youtube(video_path, title, description, tags, token_path, client_id, client_secret)` — Load Credentials.from_authorized_user_file(). If expired and has refresh_token, call creds.refresh(Request()) and save updated token. Build youtube service. Create body with snippet (title, description, tags, categoryId="22") and status (privacyStatus="public"). Use MediaFileUpload(video_path, chunksize=-1, resumable=True). Call _resumable_upload() helper. Return response["id"].

    6. `_resumable_upload(insert_request)` — Loop calling next_chunk(). On HttpError with status in {500, 502, 503, 504}, retry with exponential backoff (random.random() * 2**retry). Max 10 retries. Re-raise non-retriable errors.

    7. `upload_to_instagram(video_url, caption, ig_user_id, access_token)` — POST to /{ig_user_id}/media with media_type=REELS, video_url, caption, share_to_feed=true. Poll container status every 60s up to 5 times. On FINISHED, POST to /{ig_user_id}/media_publish. Return media ID. Raise RuntimeError on ERROR or timeout.

    8. `refresh_instagram_token_if_needed(token_path: Path)` — Load instagram_token.json, check if expires_at (stored as ISO timestamp) is within 7 days. If so, call GET https://graph.instagram.com/refresh_access_token. Save updated token + new expires_at. Return current access_token.

    9. `generate_upload_metadata(content_text, niche_hashtags, format_type)` — Call Claude Haiku (temp 0.85, max_tokens=256) with system prompt requesting JSON {"title": "...", "hashtags": ["tag1", "tag2", "tag3"]}. Title must be compelling, under 100 chars, no clickbait. Hashtags are 2-3 content-specific tags (no # prefix). Merge Claude's hashtags with niche_hashtags from channels.yaml. Return merged dict. Use existing ANTHROPIC_API_KEY from config.

    All functions use logging.getLogger(__name__). All external API calls wrapped in try/except with logging. Follow project patterns: type hints on all functions, docstrings on public functions.

    Create tests/test_upload.py with pytest. Use unittest.mock to patch google API classes, requests, and anthropic client. Use in-memory SQLite for DB tests. Test classes: TestYouTubeSetup, TestYouTubeUpload, TestInstagramUpload, TestInstagramTokenRefresh, TestMetadataGeneration, TestUploadLogging, TestUploadHistory, TestRetryBehavior.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/auto-shorts && python -m pytest tests/test_upload.py -x -q</automated>
  </verify>
  <done>pipeline/upload.py exists with all 9 functions. tests/test_upload.py passes with all test classes green. requirements.txt includes google-auth-oauthlib and google-auth-httplib2.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_upload.py -x -q` — all tests pass
- `python -c "from pipeline.upload import upload_to_youtube, upload_to_instagram, generate_upload_metadata, init_upload_table, log_upload, get_upload_history, setup_youtube_oauth, refresh_instagram_token_if_needed"` — all exports importable
- `grep google-auth-oauthlib requirements.txt` — dependency listed
</verification>

<success_criteria>
- pipeline/upload.py exists with YouTube upload, Instagram upload, metadata generation, DB logging, retry logic
- All functions have type hints and docstrings
- tests/test_upload.py has full coverage of all public functions with mocked external APIs
- requirements.txt updated with new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/04-upload-scheduler/04-01-SUMMARY.md`
</output>
